# This file contains secrets for the Streamlit frontend.
# It's used to configure the application, for example, by specifying
# the URL of the backend service.

# The URL for the InsightForge backend API.
# BACKEND_URL = "http://127.0.0.1:8000/api/v1/assistant/query"